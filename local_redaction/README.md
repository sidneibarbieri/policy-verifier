# Frozen Mapping Contract

This directory is retained in the public artifact only for `action_mapping_bank.yaml`, the frozen task-to-action mapping contract used to derive the paired human baseline.

The private raw-export anonymization workflow is intentionally not included in this package. Reviewers start from the canonical anonymized dataset already shipped under `artifact_data/dataset/`.
